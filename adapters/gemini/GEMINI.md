# NamoID Customer Identity

Use the project-local NamoID skills in `.agents/skills`. Connect to the NamoID
MCP server through Gemini's native OAuth flow. Never ask the CLI to authenticate
or expose credentials. Default configuration changes to a Test Instance and ask
for explicit confirmation before selecting or changing Live.
