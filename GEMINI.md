# NamoID Customer Identity

Use the canonical NamoID skills in `skills/` or the project-local copies in
`.agents/skills`. Connect through the host-owned OAuth flow. Never ask the CLI
for credentials or put tokens in project files. Default configuration work to a
Test Instance and require explicit user confirmation before selecting Live.
